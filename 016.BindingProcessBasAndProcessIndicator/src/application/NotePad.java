package application;

public class NotePad
{
	/*
	 * JavaFxBinding
	 * - JavaFx property binding allows you to synchronize the value  of two properties so
	 * that whenever one of the properties changes the value of the other property is updated automatically.
	 * 
	 * Unidirectional binding
	 * bidirectional binding
	 * 
	 * 
	 */
}
